info:
  name: 1 - GET health for users
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/health
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: '"curl -X GET http://localhost:3001/health"'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
