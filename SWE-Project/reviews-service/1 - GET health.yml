info:
  name: 1 - GET health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3003/health
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
