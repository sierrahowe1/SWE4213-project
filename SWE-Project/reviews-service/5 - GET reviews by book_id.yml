info:
  name: 5 - GET reviews by book_id
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3003/reviews/book/1
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
