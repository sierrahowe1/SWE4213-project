info:
  name: 2 - GET books
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3002/books
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
