info:
  name: 3 - GET books by id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3002/books/4
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
